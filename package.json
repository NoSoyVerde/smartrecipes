{
  "name": "smartrecipes",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "jsdom": "^22.0.0",
    "jest-environment-jsdom": "^29.0.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": ["**/version-jquery/**/?(*.)+(test).[jt]s?(x)"],
    "collectCoverageFrom": ["version-jquery/js/*.js"]
  }
}
